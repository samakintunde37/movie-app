import React, { Component } from "react";
import { Text, View } from "react-native";

const ShowResults = props => {
  return (
    <View>
      <Text> Show results {props.title} </Text>
    </View>
  );
};
export default ShowResults;
